For find policy, you must have the T matrix, R, discount, and a value function from value iteration.

This program (used for 2b), uses the value function and the Q function to compute the optimal policy for the given grid world.

Run it using the answer from a previous run of value iteration